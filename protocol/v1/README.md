# Protocol v1 Responsibility Index

`v1` 目前是公共协议的职责索引，不是可执行或可生成的协议规范。

- `analysis/`：分析事实的跨组件所有权边界。
- `task/`：任务身份与生命周期语义的所有权边界。
- `execution/`：执行请求、进度与结果的职责边界。
- `error/`：跨组件错误分类和兼容性职责。
- `schema/`：未来公共协议 Schema 的版本与治理职责。

这些目录不得在字段、帧格式、请求响应模型或生成工具确定前提前固化接口。
