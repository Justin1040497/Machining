# Test Fixtures

This directory is for small, public, stable fixtures used by automated tests.
Large media samples, local user files, and unclear-license files belong in
`.workspace/samples/` and must not be required by automated tests.
