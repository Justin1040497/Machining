# macOS x86_64 QMC Adapter

This directory is the build output for the Intel macOS `qmc-decrypt` slice:

```bash
scripts/build/build_qmc_decrypt_macos_arch.sh x86_64
```

The Universal 2 adapter is created by merging this output with
`../macos-arm64/`.
