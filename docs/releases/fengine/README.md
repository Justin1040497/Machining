# FEngine Releases

本目录保存独立引擎进程 FEngine 的正式版本记录和下一版本用户可见草稿。当前代码已包含 `framelean-engine` 诊断 CLI、本地 stdio Worker、分析/执行队列、运行控制和可兼容媒体的真实 libav stream-copy/remux，但没有可迁移的独立 FEngine 发布历史，因此不伪造版本记录。

下一版本用户可见草稿位于 `unreleased.md`。
