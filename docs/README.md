# Documentation

- `releases/`：按组件和版本保存正式发布记录与下一版本用户可见草稿。
- `architecture/`：跨组件架构说明。
- `development/`：Monorepo 开发与验证入口。
- `deployment/`：构建、打包和部署边界。
- `decisions/`：跨组件持久决策索引。

组件专用设计文档保留在对应组件目录；开发过程与技术流水位于根 `changelog/`。
